# Repository Notes

- Keep plugin marketplace config in `.claude-plugin/marketplace.json`.
- Keep each plugin self-contained under `plugins/<plugin-name>/`.
- Ensure each plugin has `.claude-plugin/plugin.json`.
- Place skill definitions under `plugins/<plugin-name>/skills/<skill-name>/SKILL.md`.
